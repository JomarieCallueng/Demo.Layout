 <Window x:Class="Demo.Layout.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Demo.Layout"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Window.Resources>
        <Style TargetType="Button">
            <Setter Property="Margin" Value="3"></Setter>
            <Setter Property="HorizontalAlignment" Value="Left"></Setter>
            <Setter Property="Padding" Value="3"></Setter>
            <Setter Property="Background" Value="Transparent"></Setter>
            <Setter Property="BorderThickness" Value="0"></Setter>
            <Setter Property="Foreground" Value="White"></Setter>
            <Setter Property="FontSize" Value="10"></Setter>
            <Setter Property="FontWeight" Value="Bold"></Setter>

        </Style>
    </Window.Resources>
    <DockPanel>
        <Grid DockPanel.Dock="Left"  Background="#FF343A3F">
            <Grid.RowDefinitions>
                <RowDefinition Height="40"></RowDefinition>
                <RowDefinition></RowDefinition>
            </Grid.RowDefinitions>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition></ColumnDefinition>
                    <ColumnDefinition></ColumnDefinition>
                </Grid.ColumnDefinitions>
                <Image  Margin="5" Source="/uac_logo.png"></Image>
                <TextBlock Padding="5" Foreground="White" Grid.Column="1" Text="UAC Intergrated System"
                           VerticalAlignment="Center"></TextBlock>
            </Grid>
            <Grid Grid.Row="1">
                <StackPanel>
                    <Label Foreground="White" FontWeight="Bold" Content="Register Module"></Label>
                    <Button Content="Student"></Button>
                    <Button Content="Section"></Button>
                    <Button Content="Grades Encoding"></Button>
                    <Button Content="Subject"></Button>
                    <Button Content="Reports"></Button>
                    <Button Content="Maintenance"></Button>
                    <Button Content="Announcement"></Button>
                    <Button Content="Survey"></Button>
                </StackPanel>
            </Grid>

        </Grid>
        <Grid DockPanel.Dock="Top" Height="40" Background="#FFFFC107">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="40"></ColumnDefinition>
                <ColumnDefinition></ColumnDefinition>
                <ColumnDefinition Width="40"></ColumnDefinition>
            </Grid.ColumnDefinitions>
            <TextBlock></TextBlock>
            <TextBlock VerticalAlignment="Center" HorizontalAlignment="Right"
                       Grid.Column="1" Text="John Oliver"></TextBlock>
            <TextBlock Grid.Column="2"></TextBlock>
            <Image Margin="5" Source="/menu.png"   ></Image>
            <Image Margin="8" Grid.Column="2" Source="/gear.png" ></Image>

        </Grid>
        <Grid>
        
        </Grid>
    </DockPanel>
</Window>
